import FunnelMindSidebar from "@/components/ui/sidebar-component";

export default function ShellLayout({ children }: { children: React.ReactNode }) {
  return <FunnelMindSidebar>{children}</FunnelMindSidebar>;
}
